 <%= render partial: "shared/header" %>


  <div class="main">
    <div class="container">
      <h2>Popular Shots</h2>
      <br>
      <% @shots.each do |shot| %>
      <div class="shot col-xs-2">
        <%= image_tag shot.image %>
        <h3><%= shot.name %></h3>
        <p><%= shot.description %></p>
        <%= link_to "Learn more", shot_path(shot) %>
      </div>
      <% end %>
    </div>
  </div>


<%= render partial: "shared/footer" %>